<Window x:Class="Sports_Coaches.UserMain"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        xmlns:local="clr-namespace:Sports_Coaches"
        mc:Ignorable="d"
        Title="Sports Coaches"
        Height="600"
        Width="1022"
        WindowStartupLocation="CenterScreen"
        Icon="Images/logo.jpg">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.6*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <StackPanel Margin="15">
            <StackPanel Orientation="Horizontal"
                        Margin="10">
                <TextBlock Text="Ваше місто:"
                           FontSize="15"
                           Margin="40 0 5 0"
                           Padding="10"
                           VerticalAlignment="Center" />
                <ComboBox Name="citiesCB"
                          FontSize="15"
                          Width="150"
                          VerticalAlignment="Center"
                          IsEditable="True">
                </ComboBox>
            </StackPanel>
            <TextBlock Text="Знайди свого тренера вже зараз!"
                       FontSize="23"
                       TextAlignment="Center"
                       Margin="0 20 0 20" />
            <TextBox materialDesign:HintAssist.Hint="Введіть назву спорту..."
                     Name="searchTB"
                     Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                     Width="340"
                     FontSize="15"
                     VerticalAlignment="Center"
                     TextChanged="TextBox_TextChanged" />
        </StackPanel>

        <ScrollViewer Grid.Row="2">
            <Grid Margin="10"
                  Name="sportsGrid">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                </Grid.ColumnDefinitions>

            </Grid>
        </ScrollViewer>

        <Grid Grid.RowSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="0.15*"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>
            <materialDesign:PackIcon Kind="ArrowLeft"
                                     Cursor="Hand"
                                     Margin="35,30,0,0"
                                     Height="24"
                                     Width="26" />
        </Grid>
    </Grid>

</Window>
